---
// Shared blog list-row: used on both /blogs (full list) and Home
// (recent posts preview).
import Tag from "./Tag.astro";

interface Props {
  href: string;
  title: string;
  description: string;
  date: string;
  tags: string[];
}

const { href, title, description, date, tags } = Astro.props;
---

<a href={href} class="row spotlight-item group flex items-start justify-between gap-6 py-6 first:pt-0 border-b border-white/5 transition-opacity duration-300">
  <div class="flex flex-col gap-2">
    <h3 class="text-lg text-neutral-100 group-hover:text-white transition leading-snug">{title}</h3>
    <p class="text-sm text-neutral-500 leading-relaxed max-w-md">{description}</p>
  </div>
  <div class="flex flex-col items-end gap-2 shrink-0">
    <span class="text-xs text-neutral-500">{date}</span>
    <span class="flex gap-1.5">
      {tags.slice(0, 2).map((tag) => (
        <Tag label={tag} />
      ))}
    </span>
  </div>
</a>
