// PROTOTYPE (wayfinder ticket #17) — tier -> accent color for the
// rarity-frame motif. "common" is intentionally null: most content is
// common, so the default frame stays restrained/unstyled.
export const TIER_COLORS: Record<string, string | null> = {
  common: null,
  rare: "#8b7cf6",
  epic: "#e3a63e",
};
